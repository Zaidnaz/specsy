# Reporting edges

## ADDED Requirements

### Requirement: RPT-001 Body lines report their true line number

The system MUST be robust here, on line seven of this file.

#### Scenario: works
- **THEN** it works

### Requirement: RPT-002 CLI notation is not an unfinished spec

The CLI MUST accept `--scope <name>` and resolve `<root>/changes` correctly.
The report MUST state how many documents the run covered.

#### Scenario: works
- **THEN** it works

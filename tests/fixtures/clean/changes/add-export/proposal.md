# Add CSV export

## Why

Finance reconciles invoices in a spreadsheet and currently retypes them by hand,
costing roughly four hours each month.

## What Changes

Add a CSV export button to the invoice list that downloads the current filtered
result set.

## Non-Goals

- Scheduled or emailed exports
- Formats other than CSV
- Exporting anything outside the invoice list
